// Minimal LFR case corresponding to a basic PORT path.
// Result is a direct inlet->outlet connection (PORT + CHANNEL in compiled output).
module port(
    finput in_1,
    foutput out_1
);
    assign out_1 = in_1;
endmodule
